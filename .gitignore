vault-sift/
.env
.env.*
!.env.example
node_modules/
.next/
__pycache__/
*.pyc
.venv/
dist/
# Build artifact from `npm run build:worker` (tsc -p
# lib/translation/tsconfig.worker.json) — worker.ts must ship as plain,
# directly Node-runnable JS (see lib/translation/translate.ts), so it's
# compiled ahead of time rather than committed; regenerated by
# predev/prebuild.
lib/translation/dist/
build/
.DS_Store
data/
/config/
.claude/worktrees/
*.tsbuildinfo
next-env.d.ts
graphify-out/
.claude/
